{
  "name": "ytpm-tests",
  "private": true,
  "version": "0.0.1",
  "description": "Playwright E2E tests for PlaylistPlus.user.js",
  "type": "commonjs",
  "scripts": {
    "auth": "playwright test --project=auth --headed",
    "test": "playwright test --project=ytpm",
    "test:headed": "playwright test --project=ytpm --headed",
    "test:ui": "playwright test --project=ytpm --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.0",
    "@types/node": "^22.0.0"
  }
}
